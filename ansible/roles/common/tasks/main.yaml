---
- name: Update apt cache
  apt:
    update_cache: yes
    force_apt_get: yes
    cache_valid_time: 3600

- name: Install dependencies
  apt:
    name:
      - curl
      - vim
      - htop
      - wget
      - apt-transport-https
      - ca-certificates
    state: present
